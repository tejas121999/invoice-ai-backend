/**
 * Placeholder — connect to PostgreSQL user repository later.
 */
async function listUsersPlaceholder() {
  return {
    items: [],
    note: 'MVP placeholder — no database yet',
  };
}

module.exports = {
  listUsersPlaceholder,
};
